Application works only when in terminal http-server is running. Problem with http on Chrome, which block XHR.
After run http-server in terminal, copy one of created links, and paste it into web browser.
